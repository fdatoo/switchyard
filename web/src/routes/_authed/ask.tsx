/**
 * /ask route — placeholder until Plan 09 ships the agent chat panel.
 */
import { PlaceholderPage } from "@/shell/PlaceholderPage";

export function AskPage() {
  return <PlaceholderPage title="Ask" plan="Plan 09" />;
}
